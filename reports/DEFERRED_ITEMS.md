# DEFERRED_ITEMS.md — 보류 목록 (착수 금지, 기록 전용)

DEV-04/05/06에서 "확장 후 병목 지형이 바뀌므로 보류"로 분류된 실험. **DEV-03 확장 5종 완료 → 베이스라인 v3 재측정 후 DEV-04 본 최적화 재개 시점에** 다룬다. 그 전 착수 금지.

## DEV-05 §7 보류 목록
- 요약 병렬화(`OLLAMA_NUM_PARALLEL`)
- 모델 스왑 최소화(단계 배치 재조정)
- 수집 병렬화(소스별 스레드)
- KV 캐시 양자화(`OLLAMA_KV_CACHE_TYPE`) — 확장 후 VRAM 지형 변동
- 양자화 레벨 비교(EXAONE Q4 vs Q5)
- RAG 파라미터 A/B(청크·top_k 격자)
- 저장소 보존 정책(아카이브·이미지·오디오 TTL) — 삭제는 사용자 승인 후

## DEV-06 §3 추가 보류 (신규)
- **실행 모드별 keep_alive 차등 정책**
  - 스케줄 실행(무인): 현행 **즉시 언로드 유지**가 맞음(다음 실행까지 VRAM 회수).
  - 에이전트 대화 모드(DEV-03 Phase 2): 매 질문마다 qwen2.5:14b **콜드 로드 대기** 발생(웜 브리핑 27s의 상당분이 로드 시간). → "에이전트 세션 중 keep_alive 유지 + 유휴 타임아웃 언로드" 실험으로 DEV-04 재개 시 다룬다.

## ⚠️ 불변 참고 수치 (qwen 경로 부하 추가 전 필독)
- **qwen2.5:14b 실측 10140MB, 실가용 VRAM 10832MB → 여유 692MB.**
- qwen 경로(브리핑/에이전트)에 어떤 추가 부하(더 큰 num_ctx·동시 로드·병렬)도 얹기 전에 이 여유를 확인할 것. EXAONE(6792MB)과 동시 상주 불가(합 16932MB).
- faster-whisper는 현재 CPU 실행(cublas 부재) → VRAM 0. GPU 복구(DEV-03 Phase 1.0) 시 +2.5GB로 예산 재계산 필요.
