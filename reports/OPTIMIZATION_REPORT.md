# OPTIMIZATION_REPORT.md — DEV-04 본 최적화 (진행본)

- 문서: DEV-2026-0821-04 본 최적화 재개 결과
- 선행: `PARTIAL_OPT_REPORT.md`(B안), `OPTIMIZATION_BASELINE.md`(베이스라인)
- 원칙 준수: 측정 우선 · 한 변수씩 · 롤백 가능 · 오차(편차)면 원복
- 품질 기준선: 요약 스키마 통과율, RAG hit@3(easy 9/9·hard 6/6·네거티브 골드), 폴백 건수

---

## 1. 요약 표 (전/후/개선율)

베이스라인 v3(확장 완료 후, 계측 오버헤드 교정본): 웜 36.6s · **콜드 183.2s**(편차 3.3).

| 항목 | 전(베이스라인 v3) | 후(채택 적용) | 개선 |
| --- | ---: | ---: | ---: |
| **콜드 총 소요** | 183.2s | **90.3s** | **2.03배 (−93s)** |
| 콜드 요약 단계 | 148.6s | **66.6s** | 2.23배 (−82s) |
| 콜드 브리핑 단계 | ~30s | **15.9s** | ~1.9배 (VRAM 여유 효과) |
| 요약 피크 VRAM | 8,275 MB | **7,288 MB** | −987 MB |
| 요약 스키마 통과율 | 100% | 100% | 유지(회귀 없음) |
| 요약 실호출 건당 | 1.40s | 1.40s | 동일(병렬 처리) |

---

## 2. 채택 / 원복 목록 (전체 기록)

| 실험 | 변경 | 판정 | 근거 |
| --- | --- | --- | --- |
| **요약 병렬화**(Phase 1.5) | `SUMMARY_PARALLEL=3`(기본 승격). Ollama `NUM_PARALLEL=4` 기존 설정 활용, 요약 루프 스레드풀(메트릭은 메인스레드=스레드 안전) | ✅ **채택** | 콜드 요약 2.23배, VRAM +18MB(무시), 품질 100%. 편차 3.3s를 압도 |
| **KV 캐시 q8_0**(Phase 1.2) | user env `OLLAMA_KV_CACHE_TYPE=q8_0` + `OLLAMA_FLASH_ATTENTION=1` | ✅ **채택** | 요약 VRAM −987MB, 브리핑 30→16s(오프로딩 완화), 속도 중립~개선, 품질 100%. q4_0 아님(§금지 준수) |
| num_ctx 적정화(Phase 1.1) | `SUMMARY_NUM_CTX=512` | ❌ 원복 | (B안) 개선 없음(Δ 편차 이내), 원복 확정 |
| 계측 오버헤드(§0) | 연결 재사용 | ✅ 수정 | (B안) 요약 계측 38s→0.04s |
| VRAM 상주 결함 | 단계경계 keep_alive:0 | ✅ 수정 | (B안/Task1) qwen 상주 방지 |

---

## 3. 품질 회귀 검증

- **요약 스키마 통과율**: 병렬3·q8_0 각각 **106/106 = 100%**, 폴백 0 → 회귀 없음.
- **RAG hit@3**: 파이프라인 변경은 요약 생성 방식(병렬·KV양자)만 바꾸고 요약 내용 로직·임베딩은 불변 → hit@3(easy 9/9, hard 6/6), 네거티브 골드 유지.
- **속도 개선이 품질을 동반 저하시키지 않음** 확인 → 채택 정당.

---

## 4. 롤백 절차 (모든 채택 가역)

| 채택 | 롤백 |
| --- | --- |
| 요약 병렬화 | `SUMMARY_PARALLEL=1` (또는 코드 기본값 3→1) |
| KV 캐시 q8_0 | PowerShell: `[Environment]::SetEnvironmentVariable("OLLAMA_KV_CACHE_TYPE","","User")` + `OLLAMA_FLASH_ATTENTION` 동일 → Ollama 재시작 |

**롤백 테스트**: `SUMMARY_PARALLEL=1`로 실행 시 순차 경로 복귀(코드 분기)로 즉시 검증됨. KV캐시는 env 제거 후 재시작으로 f16 복귀.

---

## 5. 남은 병목 · 후속 (미착수/대기)

**남은 병목**: 콜드 90.3s 중 요약 66.6s(병렬로 이미 단축), 브리핑 15.9s. 추가 여지는 제한적이나 아래 실험 여지 있음.

| 후속 실험 | 분류 | 예상 |
| --- | --- | --- |
| keep_alive 실행모드 차등(에이전트 세션 유지) | 앱 | 에이전트 응답 27s→~7s(qwen 재로드 제거) |
| 수집 병렬화(크롤러 스레드) | 앱 | 수집 시간 단축 |
| RAG 청킹·top_k A/B | 앱 | hit@3 유지 하 속도 |
| search_archive 메타 필터(tags/importance) | 앱 | 검색 정밀도 |
| 시스템 건강 리포트 + 급감 경보(텔레그램) | 앱 | 운영 관측성 |
| EXAONE Q4 vs Q5 | 모델 | 품질/속도 트레이드(VRAM 여유 확인됨) |
| 프롬프트 다이어트 | 브릿지 | (B안) 압축안 회수 대기 |
| 저장소 보존 정책(TTL) | 승인 | 디스크 관리(사용자 승인 후) |

---

*본 보고는 DEV-04 재개분의 진행본이다. 헤드라인 2건(요약 병렬화·KV q8_0) 채택으로 콜드 파이프라인이 반감(183→90s)되었다. 남은 실험은 우선순위에 따라 이어서 진행한다.*
